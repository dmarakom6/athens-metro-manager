
> Με τη μαγεία του [SGG Library](https://github.com/cgaueb/sgg)


## A. Περιγραφή Εφαρμογής

Το **Athens Metro Manager** είναι μια διαδραστική εφαρμογή προσομοίωσης του δικτύου μετρό. Στόχος του παιχνιδιού είναι η διαχείριση της κυκλοφορίας και η εξυπηρέτηση των επιβατών, ώστε να αποφευχθεί ο συνωστισμός (overcrowding) στους σταθμούς.

Το project υλοποιήθηκε με χρήση C++ και της βιβλιοθήκης **Simple Graphics Library (SGG)**.

### Χαρακτηριστικά
*   **Γραφική Απεικόνιση**: Το δίκτυο του μετρό απεικονίζεται ως γράφος, με σταθμούς (κόμβους) και γραμμές (ακμές).
*   **Δυναμική Προσομοίωση**: Οι συρμοί κινούνται αυτόματα, και οι επιβάτες γεννιούνται δυναμικά και αναζητούν τη βέλτιστη διαδρομή προς τον προορισμό τους.
*   **Σύστημα Βαθμολογίας**: Υπάρχει thread που τρέχει παραλλήλα και μειώνει το σκορ αν υπάρχει καθυστέρηση/συνωστισμός, επιβραβεύοντας τη σωστή διαχείριση.
*   **Interactivity**: Περιλαμβάνει διαδραστικά στοιχεία UI (κουμπιά) για τον έλεγχο της προσομοίωσης.

---

## B. Αρχιτεκτονική (Classes & Implementation)

Η εφαρμογή ακολουθεί αντικειμενοστραφή σχεδίαση με έμφαση στην κληρονομικότητα και τον πολυμορφισμό.

| Class / Component | Περιγραφή Υλοποίησης |
| :--- | :--- |
| **GlobalState** | Singleton κλάση που διαχειρίζεται την καθολική κατάσταση (level, score, running state), φορτώνει τα δεδομένα από JSON αρχεία (`jsoncpp`) και συντονίζει τον κύριο βρόχο (`init`, `update`, `draw`). Περιέχει επίσης το thread βαθμολογίας. |
| **VisualAsset** | Η βασική κλάση για όλα τα γραφικά αντικείμενα. Παρέχει την κοινή διεπαφή για `draw()` και `update()`. |
| **Station (Node)** | Κληρονομεί από `VisualAsset`. Αναπαριστά τους σταθμούς του μετρό, αποθηκεύει τους waiting passengers και διαχειρίζεται τη γραφική του αναπαράσταση. |
| **Train** | Κληρονομεί από `VisualAsset`. Υλοποιεί την κίνηση πάνω στις ακμές του γράφου και τη μεταφορά επιβατών. |
| **Passenger** | Κληρονομεί από `VisualAsset`. Οι επιβάτες έχουν κατάσταση (`WAITING`, `ON_TRAIN`, `COMPLETED`) και προορισμό. Χρησιμοποιούν αλγόριθμο δρομολόγησης για να βρουν την πορεία τους. |
| **SimulateButton** | Κληρονομεί από `VisualAsset`. Υλοποιεί λειτουργικότητα UI κουμπιών με callbacks, hover effects και λήψη mouse events. |
| **External Libs** | Χρήση της **jsoncpp** για φόρτωση δεδομένων και **SGG** για τα γραφικά. |

---

## C. Τρόπος Εκτέλεσης

Για οδηγίες μεταγλώττισης και εκτέλεσης σε διάφορα λειτουργικά συστήματα, δείτε το αρχείο `run.txt`.





